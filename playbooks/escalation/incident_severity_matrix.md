# Incident Severity Reference

Use `/docs/soc-severity-matrix.md` as the standard severity matrix.

## Fast decision cues
- Privileged account? → elevate severity
- Sensitive data? → elevate severity
- Success indicators? → elevate severity
- Multiple victims/campaign? → elevate severity
